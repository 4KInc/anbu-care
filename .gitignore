# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Env & secrets
.env
.env.local
*.pem
keys/
service-account*.json

# Local state
.anbu/
*.log
.DS_Store

# Submission working files. Kept locally, never pushed: these are drafts and
# run sheets for one person on one day, not part of what the repo is for, and
# the repo's own README should not point a stranger at a beat sheet.
docs/content/devpost-submission.md
docs/content/devto-article.md
docs/takes/recovery-run-sheet.md
